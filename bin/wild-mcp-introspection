#!/usr/bin/env ruby
# frozen_string_literal: true

# wild-mcp-introspection — standalone MCP server wrapping Wild::Introspection.
# Implemented in P2 by Role 9 (ai-engineer). Stub: exits 1 and is NOT declared
# in spec.executables until it works.

require "bundler/setup"
require "wild"

warn "wild-mcp-introspection: not implemented yet (P2, Role 9). Tracked by the bead " \
     "\"Ship plugins/wild/ marketplace-portable plugin wrapping the two MCP servers\"; " \
     "the in-process factory is Wild::Introspection::Server."
exit 1
