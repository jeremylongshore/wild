#!/usr/bin/env ruby
# frozen_string_literal: true

# wild-mcp-admin — standalone MCP server wrapping Wild::AdminTools (all calls
# pass through Wild::CapabilityGate). Implemented in P2 by Role 9. Stub: exits 1
# and is NOT declared in spec.executables until it works.

require "bundler/setup"
require "wild"

warn "wild-mcp-admin: not implemented yet (P2, Role 9). Tracked by the bead " \
     "\"Ship plugins/wild/ marketplace-portable plugin wrapping the two MCP servers\"; " \
     "the in-process factory is Wild::AdminTools::Server."
exit 1
