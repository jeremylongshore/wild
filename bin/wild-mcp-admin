#!/usr/bin/env ruby
# frozen_string_literal: true

# wild-mcp-admin — standalone MCP server wrapping Wild::AdminTools (all calls
# pass through Wild::CapabilityGate). Implemented in P2 by Role 9.

require "bundler/setup"
require "wild"

warn "wild-mcp-admin: pending P2 implementation (see build-orchestration/roles/09-mcp-ai-seams-engineer.md)"
exit 1
